-r requirements.txt
# boto3 is provided by the Lambda runtime in production but must be installed
# locally for serve.sh and the test suite.
boto3>=1.34
# moto mocks S3 in-process so the handler tests never touch a real bucket.
moto[s3]>=5.0
pytest>=8.0
